export { setupAuth, isAuthenticated, getSession } from "./replitAuth";
export { authStorage, type IAuthStorage } from "./storage";
export { registerAuthRoutes, ensureAuthenticated, getCurrentUserId, getCurrentUser } from "./routes";
